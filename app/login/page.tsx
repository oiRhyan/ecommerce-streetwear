import React from 'react'
import LoginForm from './LoginForm'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <LoginForm/>
        </div>
    )
}

export default page